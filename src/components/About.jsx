import React from "react";

const About = () => {
  return <div>This is About</div>;

  // Add a Tag Batch
  // Add a User Panel next to Logout
  // Add Toast (React Toastify)
};

export default About;
